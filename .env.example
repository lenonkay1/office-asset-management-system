# Database
DATABASE_URL=postgres://user:pass@localhost:5432/office_assets
JWT_SECRET=change_me

# SMTP (required for email notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="Office AMS <no-reply@example.com>"
# Optional default test recipient
SMTP_TEST_TO=admin@example.com

# Settings seed (optional defaults if not set in DB)
# notifications_email=true
# maintenance_reminders=true
# transfer_approvals=true
